# @next-secure-check/rules

Built-in deterministic security rules for `next-secure-check`.

Rules cover common Next.js security checks across secrets, injection, XSS, auth, config, headers, upload, and validation categories. Most users should install the CLI package instead:

```bash
npx next-secure-check scan .
```

See the main repository for rule documentation:

https://github.com/SetraTheXX/next-secure-check
